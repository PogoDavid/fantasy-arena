import React from 'react';
import './LeagueActions.css';

const LeagueActions = () => {
    return (
        <div className="league-actions">
            <button className="league-button">Create a League</button>
            <button className="league-button">Join a League</button>
        </div>
    );
};

export default LeagueActions;
